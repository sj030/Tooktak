export function SmallSection({ children }) {
  return <section className="section is-small">{children}</section>;
}
export function LargeSection({ children }) {
  return <section className="section is-large">{children}</section>;
}
